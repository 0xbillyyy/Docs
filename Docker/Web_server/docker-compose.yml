
services:
  web:
    image: nginx:latest
    ports:
      - '8080:80'
    networks:
      - internal
    volumes:
      - ./src:/var/www/html
      - ./default.conf:/etc/nginx/conf.d/default.conf

  php-fpm:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    networks:
      - internal
    volumes:
      - ./src:/var/www/html

networks:
  internal:
    driver: bridge
